import testItem from '../../../utils/test-item';
import { module, test } from 'qunit';

module('Unit | Utility | test item');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = testItem();
  assert.ok(result);
});
